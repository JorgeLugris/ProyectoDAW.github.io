const models = {
    usersModel: require('./user'),
    monitorModel: require('./monitor'),
    packageModel: require('./package'),
    reserveModel: require('./reserve'),
    blogModel: require('./blog'),
    helpModel: require('./help'),
}

module.exports = models;